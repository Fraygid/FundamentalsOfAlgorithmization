def cafe_menu():
    """
    Интерактивное меню кафе с расчетом суммы заказа
    """
    print("☕ ИНТЕРАКТИВНОЕ МЕНЮ КАФЕ")
    print("=" * 50)
    print("| №  | Напиток       | Цена       |")
    print("=" * 50)
    print("| 1  | Кофе          | 120 рублей |")
    print("| 2  | Чай           | 80 рублей  |")
    print("| 3  | Сок           | 100 рублей |")
    print("| 4  | Вода          | 50 рублей  |")
    print("| 5  | Какао         | 90 рублей  |")
    print("=" * 50)
    
    # Цены напитков
    prices = {
        1: 120,  # Кофе
        2: 80,   # Чай
        3: 100,  # Сок
        4: 50,   # Вода
        5: 90    # Какао
    }
    
    names = {
        1: "Кофе",
        2: "Чай", 
        3: "Сок",
        4: "Вода",
        5: "Какао"
    }
    
    try:
        # Ввод данных
        choice_input = input("Введите номер напитка (1-5) или название: ").strip().lower()
        
        # Определение выбора напитка
        drink_choice = None
        drink_number = None
        
        # Обработка ввода с помощью match-case
        match choice_input:
            case "1" | "кофе":
                drink_choice = "Кофе"
                drink_number = 1
            case "2" | "чай":
                drink_choice = "Чай"
                drink_number = 2
            case "3" | "сок":
                drink_choice = "Сок"
                drink_number = 3
            case "4" | "вода":
                drink_choice = "Вода"
                drink_number = 4
            case "5" | "какао":
                drink_choice = "Какао"
                drink_number = 5
            case _:
                print("❌ Ошибка: напиток не найден в меню")
                return
        
        # Ввод количества порций
        try:
            quantity = int(input("Введите количество порций: "))
            if quantity <= 0:
                print("❌ Ошибка: количество должно быть положительным числом")
                return
        except ValueError:
            print("❌ Ошибка: введите целое число для количества порций")
            return
        
        # Ввод скидки (опционально)
        discount_input = input("Введите размер скидки в % (или нажмите Enter для отсутствия скидки): ").strip()
        discount = 0
        if discount_input:
            try:
                discount = float(discount_input)
                if discount < 0 or discount > 100:
                    print("❌ Ошибка: скидка должна быть от 0 до 100%")
                    return
            except ValueError:
                print("❌ Ошибка: введите число для скидки")
                return
        
        # Расчеты
        price_per_unit = prices[drink_number]
        total_without_discount = price_per_unit * quantity
        discount_amount = total_without_discount * (discount / 100)
        final_total = total_without_discount - discount_amount
        
        # Правильное склонение слова "порция"
        if quantity == 1:
            portion_word = "порция"
        elif 2 <= quantity <= 4:
            portion_word = "порции"
        else:
            portion_word = "порций"
        
        # Вывод результата с использованием f-строк
        print("\n" + "=" * 50)
        print("🧾 ВАШ ЗАКАЗ")
        print("=" * 50)
        print(f"Напиток: {drink_choice}")
        print(f"Цена за порцию: {price_per_unit} рублей")
        print(f"Количество: {quantity} {portion_word}")
        print(f"Сумма без скидки: {total_without_discount} рублей")
        
        if discount > 0:
            print(f"Скидка: {discount}%")
            print(f"Сумма скидки: {discount_amount:.2f} рублей")
            print(f"Итоговая сумма: {final_total:.2f} рублей")
        else:
            print(f"Итоговая сумма: {final_total:.2f} рублей")
        
        print("=" * 50)
        print(f"💳 К ОПЛАТЕ: {final_total:.2f} рублей")
        print("=" * 50)
        
    except Exception as e:
        print(f"❌ Произошла ошибка: {e}")

# Функция для демонстрации работы с разными вариантами
def demo_cafe_menu():
    """
    Демонстрация работы меню с разными сценариями
    """
    print("\n" + "="*60)
    print("ДЕМОНСТРАЦИЯ РАБОТЫ МЕНЮ")
    print("="*60)
    
    # Тестовые сценарии
    test_scenarios = [
        {"input": "1", "quantity": 2, "discount": "10"},
        {"input": "чай", "quantity": 1, "discount": ""},
        {"input": "3", "quantity": 5, "discount": "15"},
    ]
    
    for scenario in test_scenarios:
        print(f"\nТест: {scenario}")
        # Здесь можно добавить автоматическое тестирование

# Запуск программы
if __name__ == "__main__":
    cafe_menu()
    
    # Раскомментируйте для демонстрации
    # demo_cafe_menu()
